apply plugin: 'maven'

task javaSourcesJar(type: Jar) {
    classifier = 'sources'
    from sourceSets.main.java.srcDirs
}

artifacts {
    archives javaSourcesJar
}

uploadArchives {
    configuration = configurations.archives
    repositories {
        mavenDeployer {
            repository(url: "http://192.168.1.155:8081/repository/android_comm/") {
                authentication(userName: "zmy", password: "111111")
            }
            pom.project {
                name 'ipc-library'
                version '1.0.3'
                artifactId 'ipc-library'
                groupId 'com.tech618'
                packaging 'aar'
                description 'ipc library'
            }
        }
    }
}